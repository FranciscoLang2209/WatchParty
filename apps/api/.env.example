# URL del proyecto Supabase (local o remoto)
SUPABASE_URL=

# Clave anónima (pública) de Supabase — nunca la service_role
SUPABASE_ANON_KEY=

# Origen exacto del frontend (Vite) permitido por CORS
WEB_ORIGIN=